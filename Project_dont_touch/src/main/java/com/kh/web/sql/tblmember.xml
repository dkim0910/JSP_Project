<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="Member">
  	<select id="checkId" parameterType="string" resultType="_int">
  		SELECT count(*) FROM user_project WHERE USERID = #{userid}
  	</select>
  
 	<insert id="join" parameterType="com.kh.web.beans.TblMember">
 		INSERT INTO user_project VALUES (#{userid},#{userpw},#{username},#{usergender},#{userphone})
 		
 	</insert>
 	<select id="login" parameterType="hashmap" resultType="com.kh.web.beans.TblMember">
 		SELECT * FROM user_project WHERE USERID = #{userid} AND USERPW = #{userpw}
 	
 	</select>
	<select id="searchByname" parameterType="string" resultType="com.kh.web.beans.TblMember">
		 SELECT * FROM user_project WHERE USERNAME = #{username} AND USERPHONE = #{userphone}
	</select>
	<select id="searchByphone" parameterType="com.kh.web.beans.TblMember" resultType="com.kh.web.beans.TblMember">
	    SELECT *
	    FROM user_project
	    WHERE username = #{username, jdbcType=VARCHAR}
	    AND userphone = #{userphone, jdbcType=VARCHAR}
	</select>	
	
  </mapper>