<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="Member">
  	<select id="checkId" parameterType="string" resultType="int">
  		SELECT count(*) FROM MEMBER WHERE USERID = #{userid}
  	</select>
  
 	<insert id="join" parameterType="userdto">
 		INSERT INTO MEMBER VALUES (#{userid},#{userpw},#{username},#{userphone},#{usergender})
 	</insert>
 	
 	<select id="login" parameterType="hashmap" resultType="userdto">
 		SELECT * FROM MEMBER WHERE USERID = #{userid} AND USERPW = #{userpw}
 	</select>
 	
	<select id="searchByname" parameterType="hashmap" resultType="userdto">
		 SELECT * FROM MEMBER WHERE USERNAME = #{username} AND USERPHONE = #{userphone}
	</select>
	
	<select id="searchByphone" parameterType="userdto" resultType="userdto">
<!-- 	<select id="searchByphone" parameterType="com.kh.web.beans.TblMember" resultType="com.kh.web.beans.TblMember"> -->
	    SELECT *
	    FROM MEMBER
	    WHERE username = #{username, jdbcType=VARCHAR}
	    AND userphone = #{userphone, jdbcType=VARCHAR}
	</select>	
	
  </mapper>