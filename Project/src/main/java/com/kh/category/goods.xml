<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="Category">
 	<select id="search" parameterType="map" resultType="goodsdto">
        SELECT * FROM PRODUCTS WHERE category = #{category} AND name = #{subcategory}
    </select>
 	<select id="Haircareinfo" resultType="goodsdto">
 		SELECT * FROM PRODUCTS WHERE PRODUCT_ID = '4308348'
    </select>
	
  </mapper>