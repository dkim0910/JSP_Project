<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
    PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-config.dtd">

<configuration>
    <settings>
        <!-- null 값을 처리할 때 사용할 기본 jdbcType 설정 -->
        <setting name="jdbcTypeForNull" value="NULL"/>
    </settings>

    <!-- 다른 MyBatis 설정 -->
</configuration>
